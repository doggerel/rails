<title>
  Chat
</title>
<ul id="chat">
  <li><%=render @messages%></li>
</ul>
<%=form_for Message.new, :html=>{:id => 'new_message'},:remote =>true do |f|%>
  <%= f.text_field :message %>
  <%= f.submit "Send" %>

<% end%>

